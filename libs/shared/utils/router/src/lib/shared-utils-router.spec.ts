import { sharedUtilsRouter } from './shared-utils-router';

describe('sharedUtilsRouter', () => {
  it('should work', () => {
    expect(sharedUtilsRouter()).toEqual('shared-utils-router');
  });
});
